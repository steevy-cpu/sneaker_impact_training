"""
label_live.py -- live shoe labeling application (PLACEHOLDER).

FUTURE PURPOSE
--------------
The main entry point operators run. It will:
    1. Open the live camera feed (camera_utils).
    2. Detect shoes with YOLO and draw bounding boxes (ui_utils).
    3. Default every shoe to "Reuse".
    4. Let the operator double-click a box to mark it "Recycle".
    5. Track shoes while in frame (tracking_utils) and, when a shoe leaves,
       save its crop + metadata JSON (save_utils).
    6. Optionally estimate shoe color (color_utils).

Implementation arrives across Phases 2-5. Nothing is implemented yet.
"""


def main():
    print("Sneaker Impact live labeling system placeholder. "
          "Implementation coming in later phases.")


if __name__ == "__main__":
    main()
