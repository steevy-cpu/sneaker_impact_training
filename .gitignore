venv/
dataset/
*.pt
*.onnx
*.engine
__pycache__/
*.pyc

# run logs
logs/

# dashboard sync ledger (local push state)
dashboard_synced.json

# macOS junk (the T7 drive is exFAT and writes these sidecar files)
.DS_Store
._*

# collected dataset images/metadata stay local
# (was "incoming_*/", which didn't match the real "incoming<MMDDYYYY>" folders)
sneaker_impact/pictures/incoming*/
models/*.pt
models/*.onnx
